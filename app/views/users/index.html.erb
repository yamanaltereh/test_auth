<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<table>
  <th>Name</th>
  <th>Email</th>
  <% @users.each do |user| %>
    <tr>
    <td><%=user.name%></td>
    <td><%=user.email%></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New user', new_user_path %>